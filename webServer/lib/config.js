const config = {
  // 启动端口
  port: 15515,

  // 数据库配置
  database: {
    DATABASE: "student",
    USERNAME: "root",
    PASSWORD: "Txyxwhodscl14.",
    PORT: "3306",
    HOST: "127.0.0.1"
  }
};

module.exports = config;
