const config = {
  // 启动端口
  port: 15515,

  // 数据库配置
  database: {
    DATABASE: "student",
    USERNAME: "root",
    PASSWORD: "5jJfg2r29d/Y",
    PORT: "3306",
    HOST: "127.0.0.1"
  }
};

module.exports = config;
